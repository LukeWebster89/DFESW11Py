# Write a Python function to sum all the numbers in a list.
 
def sum_of_all_numbers(number_input):
    total = sum(numbers)
    return total
 
numbers = [8, 2, 3, 5, 7]
print(sum_of_all_numbers(sum(numbers)))
